from setuptools import setup

setup(
    name='Health-Care-Application-Deployement',
    packages=['Health-Care-Application-Deployement'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
)