from setuptools import setup

setup(
    name='Health_Care_Application_Deployement',
    packages=['Health_Care_Application_Deployement'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
)